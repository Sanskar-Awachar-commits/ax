name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: ax-linux
            binary_name: ax
          - os: macos-latest
            artifact_name: ax-macos
            binary_name: ax
          - os: windows-latest
            artifact_name: ax-windows.exe
            binary_name: ax.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compile on Linux / macOS
        if: runner.os != 'Windows'
        run: |
          c++ -O3 -std=c++17 ax.cpp -o ${{ matrix.binary_name }}
          mv ${{ matrix.binary_name }} ${{ matrix.artifact_name }}

      - name: Compile on Windows
        if: runner.os == 'Windows'
        run: |
          g++ -O3 -std=c++17 ax.cpp -o ${{ matrix.binary_name }}
          Move-Item -Path ${{ matrix.binary_name }} -Destination ${{ matrix.artifact_name }}

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release_assets
          merge-multiple: true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release_assets/*
          generate_release_notes: true